# Acceptance and test matrix

Status values: `planned`, `implemented`, `passing-local`, `passing-docker`, `passing-macos`, `manual-passed`, `blocked-external`.

| Area | Acceptance behavior | Automated coverage | Manual/E2E coverage | Status |
|---|---|---|---|---|
| Product identity | Miravo is the display/API/artifact name while the stable internal module stays `ProjectLedger`; bundle ID remains centralized and provisional | public-config test, iOS source contract, unsigned workflow metadata checks | Confirm installed app label and signed bundle ID | passing-local / blocked-external |
| Health | Liveness avoids dependencies; readiness checks DB/cache | `backend/tests/test_health.py` | Compose curl smoke test | passing-local |
| Login | Email/password creates a device session; no raw password/token logged; native login uses a bundled default server URL when configured, hides the server from the main login UI, and keeps manual URL entry as an unconfigured/settings fallback | `backend/tests/test_auth.py`; native source contract/localization checks | Owner login from iPhone against the bundled public URL | passing-local / implemented |
| Refresh | One-time rotation; reuse revokes session/family | `backend/tests/test_auth.py` | Retry with captured synthetic token | passing-local |
| Sessions | List own devices; revoke another or current session | `backend/tests/test_auth.py` | Settings device-session UI | passing-local |
| Bootstrap and test users | First owner only; normal non-admin app users can be created server-side without public registration or command-line password arguments; env values cleared; no hardcoded secret | `backend/tests/test_bootstrap_owner.py` | Ubuntu bootstrap and test-user runbook | passing-local |
| Errors/logging | Stable code/message/details/request ID; redaction | `backend/tests/test_errors_and_logging.py` | Inspect support bundle | passing-local |
| Permissions | Owner/admin/editor/viewer object boundaries | `test_tracker_api.py` role matrix/invites/ownership | Two-user device flow | passing-local |
| iOS role boundary | Server membership role updates the scoped tracker cache; viewers cannot append financial/taxonomy/outbox mutations; owner/admin online controls create one-time invites and change/remove eligible members | `LocalLedgerRepositoryTests.swift`, `LedgerSyncActorTests.swift`, `CollaborationTests.swift` (authored), collaboration source contract | Role change while offline/foreground and two-user invitation acceptance | implemented |
| iOS tracker settings | Owner/admin presentation, defaults, archive, and shared ordering changes validate before one local commit; editor/viewer attempts append no outbox work | `LocalLedgerRepositoryTests.swift` (authored) | Reorder mixed trackers and retain an archived current default | implemented |
| Money | ISO code/exponent and integer minor-unit validation | `test_currency.py`, transaction serializer tests | Locale UI checks | passing-local |
| iOS money input | Strict ASCII digits, locale decimal separator, precision/overflow/currency-lookalike rejection | `MoneyTests.swift` (authored) | Albanian/English keypad on device | implemented |
| iOS local ledger | Compound server/user-scoped identity; create/edit/tombstone/restore/undo commits entity, derived transfer/refund movements, conversion snapshot, allocations, strict snake-case payload, and ordered outbox atomically | `LocalLedgerRepositoryTests.swift` including forced outbox-failure rollback (authored) | Airplane-mode quick add/transfer/refund/relaunch | implemented |
| iOS balances | Opening balance plus posted local movements; deleted/void/draft/pending exclusion | `LocalBalanceCalculatorTests.swift` (authored) | Compare Overview/account values | implemented |
| iOS session security | HTTPS/release policy, loopback-only debug HTTP, JWT scope, Keychain round-trip, preference isolation | `ServerURLPolicyTests.swift`, `KeychainSessionTokenStoreTests.swift`, `AppPreferencesTests.swift` (authored) | Face ID/passcode and expired-token checks | implemented |
| iOS store failure | Persistent store is never automatically replaced; blocking recovery state uses only temporary memory | source contract; macOS failure-injection test pending | Preserve a device container and recover | implemented |
| Accounts | Derived balances, transfer links, cross-currency snapshots | `test_ledger_transactions.py` | Compare ledger totals | passing-local |
| Transactions | Refund/void/allocation/audit/tombstone/revision semantics; sync transfer/refund movement parity and claimed base-currency validation | `test_ledger_transactions.py`, `test_taxonomy_and_audit.py`, `test_sync.py` | Offline quick-add/undo scenario | passing-local |
| Tags | Tracker-scoped normalized uniqueness, explicit transaction links, offline CRUD/outbox, search/filter, bootstrap/pull/tombstone, and preservation of an existing archived tag without allowing new assignment | `test_sync.py`, `LocalLedgerRepositoryTests.swift`, `LedgerSyncActorTests.swift` (native authored) | Create/archive/edit/restore tag flow | passing-local |
| Transaction discovery | Debounced local search over related names/tags/amount and combined tracker/account/category/tag/type/source/status/sync/currency/date facets; currencies remain separate in totals | `TransactionListQueryTests.swift`, `TransactionListPerformanceTests.swift` 50k regression (authored) | VoiceOver/filter reset and representative-device responsiveness | implemented |
| Sync server | Ordered per-operation push, tracker/account/category/tag/transaction replay/fingerprint safety, signed user-bound cursor, paging/tombstone/paginated-bootstrap/ack, stable bootstrap target token, retention, revocation filtering | `test_sync.py` (11 tests) | Docker/PostgreSQL concurrency smoke test | passing-local |
| Sync iOS | Stable operation IDs, one same-entity operation per batch, sequential rebase, retry/backoff, atomic pull/cursor rollback, resumable staging, memberships/tags/transaction links, post-bootstrap pull, token rotation, and first-bootstrap starter provisioning only after a confirmed empty restore | `LedgerSyncActorTests.swift`, `SyncWireModelsTests.swift` (authored), source contract/localization checks | Airplane-mode/reinstall scenarios | implemented |
| Conflict | Server preserves current state/local proposal while siblings progress; client stores field review and can keep server or resubmit mine with a new operation ID | `test_sync.py`, `LedgerSyncActorTests.swift` (authored) | Two-client review screen | implemented |
| Realtime invalidation | Same active device-session authentication; sequence-only frames; token-expiry close; redirect refusal; bounded decode; polling remains correct | `test_realtime.py` (3 passing), `SyncInvalidationClientTests.swift` (authored) | Redis outage and foreground reconnect | implemented |
| Sync triggers | Initial path is ignored; connectivity return coalesces a pull; background request is best effort and expiration-cancellable | `SyncTriggerTests.swift` (authored), source/plist contract | Background-refresh disabled and signer-altered capability checks | implemented |
| Attachment queue boundary | Identical enqueue is idempotent; unsafe path/thumbnail/MIME/size/digest/cross-scope reference fails; interrupted transfer survives restart; batches are bounded; pending cancellation retains the protected local receipt and explicit retry resumes it | `AttachmentTransferQueueTests.swift`, `AttachmentTransferWorkerTests.swift` (authored) | Relaunch with pending local receipt, retry, cancel/resume | implemented |
| Shortcut server | Independent hash/pepper, one-time raw display, tracker/scopes/current role, expiry/revoke/audit, payment-credential rejection, explicit conversion, auth/token/user throttles, user-scoped replay/fingerprint conflict, partial batch, expiry cleanup | `test_shortcut_api.py` (10 passing locally) | Online and queued repeated capture against deployed host | passing-local |
| Shortcut app/setup | Create/list/replace/revoke a tracker-scoped token; expose tracker defaults; never persist/reconstruct/log raw value; local-only pasteboard expires; reproducible online and bounded offline-queue construction; after sync, Shortcut-created posted expenses can raise a Miravo local notification with amount and merchant once per transaction | `ShortcutCredentialTests.swift` and `RecurringReminderControllerTests.swift` (authored), iOS privacy contract, checked-in single/batch JSON samples | macOS compile/UI plus current-iPhone Wallet trigger, Miravo local notification delivery, duplicate notification, queue rewrite/flush | implemented |
| Budgets | Posted expense only; tracker/category scope; DST-safe monthly/weekly/custom periods; signed rollover; thresholds; category snapshots; explicit partial conversion; offline sync/progress | `test_budgets.py` (5 passing), `LocalBudgetTests.swift`, `LedgerSyncActorTests.swift` (native authored) | Month boundary, category rename, partial-rate, archive/restore UI | passing-local / implemented |
| Recurrence | Month-end/leap/DST anchors; pause/resume/skip/edit-future/end; bounded downtime catch-up; stable keys; retry recovery; posted-history preservation; subscription metadata; scoped offline cache/outbox; corrupt-response rejection; explicit reminder opt-in/denial; generic content; opaque IDs; one deterministic 50-request recurrence/installment bound | `test_recurring.py` (7 passing), Celery task, REST/sync/OpenAPI; `LocalRecurringTests.swift`, `LedgerSyncActorTests.swift`, `SyncWireModelsTests.swift`, `RecurringReminderPlannerTests.swift`, `RecurringReminderControllerTests.swift` (native authored); Linux privacy contract | Native rule/occurrence Plans flow, notification authorization/delivery/revocation, and Redis worker downtime | passing-local (backend) / implemented (iOS source) |
| Installments | Exact component schedule and cross-platform UUIDv5 row identity; month-end/leap anchors; immutable revisions; atomic offline plan/ledger projection; regular/partial/extra allocation; skip/reschedule; explicit account/base conversion; early payoff/overpayment; role/version/idempotency/rejection/conflict/bootstrap/tombstone behavior; preserved local projections still validate every skipped remote snapshot; server-authored payment history | `test_installments.py` (6 passing), REST/sync/OpenAPI/fresh migration; `LocalInstallmentTests.swift`, `LedgerSyncActorTests.swift`, `SyncWireModelsTests.swift`, `RecurringReminderPlannerTests.swift`, `RecurringReminderControllerTests.swift` (native authored); Linux installment/privacy contracts | Airplane-mode create/edit/multiple-payment/relaunch, two-client conflict choices, account conversion, payoff, reminder delivery, Dynamic Type/VoiceOver | passing-local (backend) / implemented (iOS source) |
| Splits and settlements | Registered/guest participant lifecycle; multiple payers; exact/equal/percentage totals and deterministic minor-unit rounding; zero-sum simplified debts; settlements reduce only current debt; linked movements follow settlement tombstone/restore; guest merge preserves amounts/revisions; role enforcement; replay-safe push/bootstrap | `test_splits_and_settlements.py` (5 passing), relational constraints, REST/sync/OpenAPI; `LocalSplittingTests.swift`, `LedgerSyncActorTests.swift`, `SyncWireModelsTests.swift`, `CollaborationTests.swift` (native authored); Linux collaboration/localization contracts | Airplane-mode split/settle, guest merge/invite, two-device and no-WebSocket flow | passing-local (backend) / implemented (iOS source) |
| Receipt server | Client-UUID reservation is fingerprint-idempotent; bytes are streamed, bounded, sniffed, size/digest checked, private/quarantined; viewer/editor/object boundaries and transaction-cascade tombstones hold; metadata sync never exposes storage keys | `backend/tests/test_attachments.py` (5 passing), migration/OpenAPI/sync tests | Scanner deployment and unauthorized external probe | passing-local |
| Receipt app | Camera/photo/file/PDF capture prepares a metadata-stripped derivative and thumbnail locally first; Vision suggestions never apply without editable confirmation; OCR failure still attaches; queue upload/download/restart/cancel is checksum-bound; local and authenticated cross-device previews are private | `ReceiptOCRTests.swift`, `ReceiptPreparationServiceTests.swift`, `AttachmentTransferQueueTests.swift`, `AttachmentTransferWorkerTests.swift`, `LedgerSyncActorTests.swift` (authored), Linux privacy/transport contract | Camera/photo/files permission, OCR correction, airplane-mode relaunch, second-device private preview, quarantine, Dynamic Type/VoiceOver | implemented / blocked-external |
| Currency | Manual snapshots are required and immutable; local decimal snapshot creation; server base-currency and amount/rate consistency validation; provider abstraction/report caveats remain | `test_currency.py`, `MoneyTests.swift`, cross-currency API/sync tests | Partially unconverted report | implemented |
| Analytics | Offline/server totals consistent; transfers/settlements/voids excluded; refunds net original spending; historical snapshots only; missing conversions explicit; trend/category/merchant/source/account/budget/subscription/installment/split summaries carry accessible labels | `backend/tests/test_analytics.py` (3 passing), `LocalAnalyticsCalculatorTests.swift` (authored), Insights source contract | 50k record simulator check and server/UI total comparison | passing-local (backend) / implemented (iOS source) |
| Exports | UTF-8 CSV, valid PDF/full JSON portability, private random storage, authenticated requester-only expiry, checksum header, audit, stable filters, no storage-key leak, native same-origin/size/checksum/content-type verified download handoff | `backend/tests/test_exports.py` (3 passing), migration/OpenAPI, native export source contract | Native export UI runtime and UI total comparison | passing-local (backend) / implemented (iOS source) |
| Localization | Every implemented user string exists in English and Albanian | `check-localizations.sh` and 783-key coverage/format check | Native-speaker review | passing-local |
| iOS privacy/transport | Release has HTTPS-only ATS, Debug loopback exception, CA92.1, accurate linked-data/no-tracking declaration, private attachment/export download contracts | `check-project-contract.py` | Inspect generated Xcode privacy report | passing-local |
| Accessibility | Dynamic Type, VoiceOver labels, non-color status/read-only cues, contrast/reduced motion, touch targets | Source audit in `docs/accessibility-audit.md`; macOS tests pending | Physical-device audit | implemented |
| Backup | DB/media/config manifest, checksum, encryption, retention, isolated restore | Milestone 10 restore CI/script | Disaster-recovery drill | planned |
| Exposure | Public API only; no DB/Redis/admin/metrics/debug/raw media | Proxy policy tests | External port/path probe | planned |
| iOS CI | Regenerate project, simulator build/test, no secrets | `.github/workflows/ios-ci.yml`; tests authored | Review xcresult | blocked-external |
| Unsigned IPA | Miravo device app, `Payload/Miravo.app` structure, display/bundle/version metadata, architectures, SHA-256/manifest | `.github/workflows/unsigned-ipa.yml` | Signer + device checklist | implemented / blocked-external |

## Required end-to-end scenarios

All twelve scenarios from `PROMPT.md` remain required: airplane-mode mutation, repeated Shortcut, app-closed Shortcut, offline queue replay, two-client conflict, role/split/settlement, scheduler downtime, private OCR receipt, matching CSV/PDF, reinstall/bootstrap, isolated backup restore, and unsigned-IPA/device smoke testing.
